# 🔧 КРИТИЧЕСКИЕ ИСПРАВЛЕНИЯ ARBAREA MOBILE APP

## Дата: 2026-01-03

---

## ✅ ИСПРАВЛЕНО АВТОМАТИЧЕСКИ:

### 1. Карта не работала

**Проблема:** Отсутствовал CSS для Leaflet библиотеки
**Решение:** Добавлен `leaflet.css` в `index.html`

```html
<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
```

### 2. Маркеры на карте не отображались

**Проблема:** React-Leaflet не загружает иконки маркеров по умолчанию в Vite/Webpack
**Решение:** Добавлен fix иконок в `DeliverySelectorWithMap.jsx`

### 3. Кнопка "Заказать" в корзине была обрезана

**Проблема:** Неправильное позиционирование fixed панели (bottom-24) без учета safe-area
**Решение:** Исправлено позиционирование с `calc(5rem + env(safe-area-inset-bottom))`

---

## ⚠️ ТРЕБУЕТСЯ РУЧНАЯ НАСТРОЙКА:

### 4. Тинькофф оплата - КРИТИЧНО!

**Проблема:** Переменные окружения в `.env` используют неправильные имена:

- Текущие: `VITE_TINKOFF_TERMINAL_KEY`, `VITE_TINKOFF_SECRET`
- Требуются API: `TINKOFF_TERMINAL_KEY`, `TINKOFF_PASSWORD`

**⚡ РЕШЕНИЕ:** Добавьте в ваш `.env` файл:

```env
# ================================
# СЕРВЕРНЫЕ ПЕРЕМЕННЫЕ (для API)
# ================================

# Supabase Admin
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...

# Tinkoff Payments (БЕЗ VITE_ ПРЕФИКСА!)
TINKOFF_TERMINAL_KEY=1764148402246
TINKOFF_PASSWORD=SDxEmw0fb2$gRv*6

# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```

**Важно:**

- `VITE_` префикс = доступно в браузере (НЕБЕЗОПАСНО для секретов!)
- Без `VITE_` = только сервер (безопасно)

---

## 📋 CHECKLIST ДЛЯ ПРОВЕРКИ:

### После исправлений проверьте:

- [ ] **Карта**: Откройте выбор доставки → карта должна отображаться
- [ ] **Кнопка корзины**: Добавьте товар → откройте корзину → кнопка "Оформить заказ" полностью видна
- [ ] **Оплата**: Оформите тестовый заказ → проверьте редирект на Тинькофф
- [ ] **Маркеры**: На карте при выборе адреса должен появляться маркер

---

## 🔍 Диагностика Тинькофф

Если оплата не работает, запустите:

```bash
node scripts/validate-env.js
```

Или проверьте вручную в Vercel Dashboard:

1. Settings → Environment Variables
2. Убедитесь что есть:
   - `TINKOFF_TERMINAL_KEY`
   - `TINKOFF_PASSWORD`
   - `SUPABASE_SERVICE_ROLE_KEY`
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`

---

## 📁 ИЗМЕНЕННЫЕ ФАЙЛЫ:

1. `index.html` - добавлен Leaflet CSS
2. `src/pages/Cart.jsx` - исправлено позиционирование кнопки
3. `src/components/features/DeliverySelectorWithMap.jsx` - исправлены иконки маркеров
