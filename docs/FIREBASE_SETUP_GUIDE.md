# ТЕХНИЧЕСКОЕ ЗАДАНИЕ: Настройка Firebase Console для Arbarea Mobile App

**Дата создания:** 2025-11-25  
**Проект:** arbarea-mobile-app  
**Цель:** Настроить Firebase Authentication, Firestore, Storage для корректной работы приложения

---

## 📋 ПРЕДВАРИТЕЛЬНЫЕ ТРЕБОВАНИЯ

- Аккаунт Google
- Доступ к Firebase Console: https://console.firebase.google.com/
- Проект Firebase уже создан (судя по наличию ключей в .env)

---

## 🔥 ЧАСТЬ 1: AUTHENTICATION (Аутентификация)

### Шаг 1.1: Включить Email/Password провайдер

1. Откройте Firebase Console: https://console.firebase.google.com/
2. Выберите проект **arbarea-mobile-app** (или ваш проект)
3. В левом меню выберите **Authentication** (🔐 Аутентификация)
4. Перейдите на вкладку **Sign-in method** (Способы входа)
5. Найдите **Email/Password** в списке провайдеров
6. Нажмите на него, затем нажмите **Enable** (Включить)
7. Убедитесь, что переключатель **Email/Password** включен (синий)
8. **НЕ включайте** "Email link (passwordless sign-in)" - это не нужно
9. Нажмите **Save** (Сохранить)

**Ожидаемый результат:**  
✅ Email/Password должен быть в статусе "Enabled" (Включено)

---

### Шаг 1.2: Включить Google Sign-In (опционально, но рекомендуется)

1. В той же вкладке **Sign-in method** найдите **Google**
2. Нажмите на него, затем **Enable**
3. Введите:
   - **Project support email**: ваш email (например, admin@arbarea.com)
4. Нажмите **Save**

**Ожидаемый результат:**  
✅ Google должен быть в статусе "Enabled"

---

### Шаг 1.3: Включить Anonymous Authentication (для диагностики)

1. В той же вкладке найдите **Anonymous**
2. Нажмите **Enable**
3. Нажмите **Save**

**Зачем это нужно:**  
Это позволит странице `/diagnostics` проверять соединение с Firebase без регистрации.

---

### Шаг 1.4: Настроить Authorized Domains (Разрешенные домены)

1. В разделе **Authentication** перейдите на вкладку **Settings** (Настройки)
2. Прокрутите до раздела **Authorized domains** (Разрешенные домены)
3. Убедитесь, что в списке есть:
   - `localhost` (для локальной разработки)
   - `arbarea-misdt0eqp-neuroexpert.vercel.app` (ваш Vercel домен)
   - Любые другие домены, где будет развернуто приложение

4. Если домена нет, нажмите **Add domain** и добавьте его

**Ожидаемый результат:**  
✅ Все домены приложения должны быть в списке

---

## 🗄️ ЧАСТЬ 2: FIRESTORE DATABASE (База данных)

### Шаг 2.1: Создать/Проверить Firestore Database

1. В левом меню выберите **Firestore Database** (☁️ База данных Firestore)
2. Если база данных еще не создана:
   - Нажмите **Create database**
   - Выберите режим: **Start in production mode** (Начать в режиме production)
   - Выберите регион: **europe-west** (ближайший к вашим пользователям)
   - Нажмите **Enable**

**Ожидаемый результат:**  
✅ Firestore Database создана и активна

---

### Шаг 2.2: Настроить Security Rules (Правила безопасности)

1. В Firestore Database перейдите на вкладку **Rules** (Правила)
2. Замените существующие правила на следующие:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Правило для коллекции products (товары)
    match /products/{productId} {
      allow read: if true; // Все могут читать товары
      allow write: if request.auth != null && request.auth.token.admin == true; // Только админы могут редактировать
    }
    
    // Правило для коллекции orders (заказы)
    match /orders/{orderId} {
      allow read: if request.auth != null && request.auth.uid == resource.data.userId; // Пользователь видит только свои заказы
      allow create: if request.auth != null; // Авторизованные пользователи могут создавать заказы
      allow update, delete: if request.auth != null && request.auth.token.admin == true; // Только админы могут редактировать
    }
    
    // Правило для коллекции individual-orders (индивидуальные заявки)
    match /individual-orders/{orderId} {
      allow read: if request.auth != null && (request.auth.uid == resource.data.userId || request.auth.token.admin == true);
      allow create: if request.auth != null;
      allow update, delete: if request.auth != null && request.auth.token.admin == true;
    }
    
    // Правило для коллекции users (пользователи)
    match /users/{userId} {
      allow read: if request.auth != null && request.auth.uid == userId; // Пользователь видит только свой профиль
      allow write: if request.auth != null && request.auth.uid == userId; // Пользователь может редактировать только свой профиль
    }
    
    // Правило для коллекции cart (корзина)
    match /cart/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

3. Нажмите **Publish** (Опубликовать)

**Ожидаемый результат:**  
✅ Правила опубликованы без ошибок

---

### Шаг 2.3: Создать необходимые коллекции (опционально)

Firestore создаст коллекции автоматически при первой записи, но можно создать их вручную:

1. Перейдите на вкладку **Data** (Данные)
2. Нажмите **Start collection** (Начать коллекцию)
3. Создайте следующие коллекции:
   - `products` (товары)
   - `orders` (заказы)
   - `individual-orders` (индивидуальные заявки)
   - `users` (пользователи)
   - `cart` (корзина)

**Примечание:** Это необязательно, приложение создаст их автоматически.

---

## 📦 ЧАСТЬ 3: STORAGE (Хранилище файлов)

### Шаг 3.1: Создать/Проверить Storage

1. В левом меню выберите **Storage** (📁 Хранилище)
2. Если хранилище еще не создано:
   - Нажмите **Get started**
   - Выберите режим: **Start in production mode**
   - Выберите регион: **europe-west** (тот же, что и для Firestore)
   - Нажмите **Done**

**Ожидаемый результат:**  
✅ Storage создан и активен

---

### Шаг 3.2: Настроить Security Rules для Storage

1. В Storage перейдите на вкладку **Rules** (Правила)
2. Замените существующие правила на следующие:

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    
    // Правило для папки products (изображения товаров)
    match /products/{allPaths=**} {
      allow read: if true; // Все могут читать изображения товаров
      allow write: if request.auth != null && request.auth.token.admin == true; // Только админы могут загружать
    }
    
    // Правило для папки individual-orders (файлы индивидуальных заявок)
    match /individual-orders/{userId}/{allPaths=**} {
      allow read: if request.auth != null && (request.auth.uid == userId || request.auth.token.admin == true);
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Правило для папки user-uploads (загрузки пользователей)
    match /user-uploads/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

3. Нажмите **Publish**

**Ожидаемый результат:**  
✅ Правила опубликованы без ошибок

---

## 🔧 ЧАСТЬ 4: PROJECT SETTINGS (Настройки проекта)

### Шаг 4.1: Проверить конфигурацию приложения

1. В левом меню нажмите на шестеренку ⚙️ → **Project settings** (Настройки проекта)
2. Прокрутите до раздела **Your apps** (Ваши приложения)
3. Найдите ваше веб-приложение (значок `</>`):
   - Если его нет, нажмите **Add app** → **Web** и создайте его
4. Убедитесь, что конфигурация совпадает с вашим `.env` файлом:

```javascript
const firebaseConfig = {
  apiKey: "AIzaSy...", // должен совпадать с VITE_FIREBASE_API_KEY
  authDomain: "arbarea-app.firebaseapp.com", // должен совпадать с VITE_FIREBASE_AUTH_DOMAIN
  projectId: "arbarea-app", // должен совпадать с VITE_FIREBASE_PROJECT_ID
  storageBucket: "arbarea-app.appspot.com", // должен совпадать с VITE_FIREBASE_STORAGE_BUCKET
  messagingSenderId: "123456789", // должен совпадать с VITE_FIREBASE_MESSAGING_SENDER_ID
  appId: "1:123456789:web:abc123" // должен совпадать с VITE_FIREBASE_APP_ID
};
```

**Ожидаемый результат:**  
✅ Все ключи совпадают

---

## ✅ ПРОВЕРКА НАСТРОЙКИ

После выполнения всех шагов, откройте приложение и проверьте:

1. **Диагностика Firebase:**
   - Откройте страницу `/diagnostics` (или временно установите `activeTab = 'diagnostics'` в App.jsx)
   - Вы должны увидеть:
     ```
     ✅ Config found: VITE_FIREBASE_API_KEY
     ✅ Config found: VITE_FIREBASE_AUTH_DOMAIN
     ✅ Config found: VITE_FIREBASE_PROJECT_ID
     ✅ Anonymous Login Successful! Firebase connection is working.
     ```

2. **Регистрация пользователя:**
   - Перейдите в профиль (вкладка "Профиль")
   - Нажмите "Войти через Email"
   - Выберите "Нет аккаунта? Создать"
   - Введите email и пароль
   - Нажмите "Зарегистрироваться"
   - **Ожидаемый результат:** Регистрация проходит успешно, вы авторизованы

3. **Добавление товара в корзину:**
   - Откройте любой товар
   - Нажмите "Добавить в корзину"
   - **Ожидаемый результат:** Товар добавлен, счетчик корзины увеличился

---

## 🚨 TROUBLESHOOTING (Решение проблем)

### Проблема: "auth/operation-not-allowed"
**Решение:** Email/Password провайдер не включен. Вернитесь к Шагу 1.1

### Проблема: "auth/unauthorized-domain"
**Решение:** Домен не добавлен в Authorized Domains. Вернитесь к Шагу 1.4

### Проблема: "permission-denied" при записи в Firestore
**Решение:** Проверьте Security Rules в Firestore. Вернитесь к Шагу 2.2

### Проблема: "Illegal url for new iframe"
**Решение:** Добавьте `*.firebaseapp.com` в CSP headers (уже исправлено в vercel.json)

---

## 📞 КОНТАКТЫ ДЛЯ ПОДДЕРЖКИ

Если возникли проблемы:
1. Проверьте консоль браузера (F12) на наличие ошибок
2. Откройте страницу `/diagnostics` для проверки соединения
3. Запустите `npm run check` для проверки целостности проекта

---

**Документ создан:** 2025-11-25  
**Версия:** 1.0  
**Статус:** Готов к использованию
