# 🔌 Руководство по подключению API

Это руководство поможет настроить все интеграции для полноценной работы Arbarea.

---

## 📋 Оглавление

1. [Email-уведомления (Resend)](#1-email-уведомления-resend)
2. [Платёжный webhook (Тинькофф)](#2-платёжный-webhook-тинькофф)
3. [СДЭК API (виджет и ПВЗ)](#3-сдэк-api-виджет-и-пвз)
4. [Boxberry API](#4-boxberry-api)
5. [Telegram Bot](#5-telegram-bot)
6. [Переменные окружения](#6-переменные-окружения)

---

## 1. Email-уведомления (Resend)

### Шаг 1: Регистрация и получение API-ключа

1. Перейдите на [resend.com](https://resend.com) и зарегистрируйтесь
2. Подтвердите email
3. Перейдите в **API Keys** → **Create API Key**
4. Назовите ключ (например, `arbarea-production`)
5. Скопируйте полученный ключ (начинается с `re_`)

### Шаг 2: Добавление переменной окружения

```bash
# В Vercel Dashboard → Settings → Environment Variables
RESEND_API_KEY=re_xxxxxxxxxxxxx
```

**Всё! Email уже работает!** 🎉

По умолчанию письма отправляются с адреса `onboarding@resend.dev`. Это работает сразу без настройки DNS.

### Шаг 3 (опционально): Настройка своего домена

Если хотите отправлять с вашего домена (например, `orders@arbarea.ru`):

1. Вам нужен **собственный домен** (не поддомен Vercel)
2. В Resend перейдите в **Domains** → **Add Domain**
3. Введите ваш домен
4. Добавьте DNS-записи, которые предложит Resend
5. Дождитесь верификации (до 24 часов)
6. Добавьте переменную:
   ```bash
   RESEND_FROM_EMAIL=Arbarea <orders@arbarea.ru>
   ```

> ⚠️ **Важно**: Поддомены Vercel (типа `arbarea-bice.vercel.app`) нельзя верифицировать, так как вы не контролируете их DNS-записи.

### Шаг 4: Тестирование

```bash
curl -X POST https://arbarea.ru/api/send-email \
  -H "Content-Type: application/json" \
  -d '{
    "type": "order_created",
    "orderId": "TEST-123",
    "email": "your@email.com",
    "name": "Тест"
  }'
```

---

## 2. Платёжный Webhook (Тинькофф)

### Шаг 1: Вход в личный кабинет

1. Откройте [business.tbank.ru](https://business.tbank.ru)
2. Войдите в аккаунт магазина
3. Перейдите в раздел **Интернет-эквайринг** → **Терминалы**

### Шаг 2: Настройка webhook URL

1. Выберите ваш терминал
2. Найдите раздел **Notification URL** (или **Webhook URL**)
3. Укажите URL:
   ```
   https://arbarea.ru/api/payment-webhook
   ```
4. Сохраните настройки

### Шаг 3: Настройка уведомлений

Включите уведомления для статусов:

- ✅ `AUTHORIZED` — платёж авторизован
- ✅ `CONFIRMED` — платёж подтверждён
- ✅ `REVERSED` — возврат
- ✅ `REFUNDED` — возврат средств
- ✅ `REJECTED` — платёж отклонён
- ✅ `CANCELED` — платёж отменён

### Шаг 4: Проверка подписи (уже реализовано)

Наш webhook автоматически проверяет подпись токена через SHA-256. Убедитесь, что переменные окружения настроены:

```bash
TINKOFF_TERMINAL_KEY=ваш_терминал_ключ
TINKOFF_PASSWORD=ваш_пароль
```

### Шаг 5: Тестирование

1. Создайте тестовый заказ
2. Оплатите тестовой картой: `4300000000000777`
3. Проверьте логи в Vercel: должно появиться сообщение о webhook
4. Проверьте статус заказа в Firestore

---

## 3. СДЭК API (виджет и ПВЗ)

### Шаг 1: Регистрация как интернет-магазин

1. Перейдите на [cdek.ru/ru/integration](https://www.cdek.ru/ru/integration)
2. Нажмите **Стать клиентом** или **Подключиться**
3. Заполните форму регистрации ИП/ООО
4. Дождитесь одобрения (1-3 рабочих дня)

### Шаг 2: Получение API-ключей

1. Войдите в личный кабинет СДЭК
2. Перейдите в **Интеграции** → **API-ключи**
3. Создайте новый ключ (Test и Production)
4. Скопируйте:
   - `Client ID` (Account)
   - `Client Secret` (Secure password)

### Шаг 3: Добавление переменных окружения

```bash
CDEK_CLIENT_ID=ваш_client_id
CDEK_CLIENT_SECRET=ваш_client_secret
CDEK_TEST_MODE=false  # true для тестового режима
```

### Шаг 4: Создание API-эндпоинта

Создайте файл `api/cdek-points.js`:

```javascript
// api/cdek-points.js
export default async function handler(req, res) {
  const { city, type } = req.query;

  // Получение токена
  const tokenResponse = await fetch('https://api.cdek.ru/v2/oauth/token', {
    method: 'POST',
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    body: new URLSearchParams({
      grant_type: 'client_credentials',
      client_id: process.env.CDEK_CLIENT_ID,
      client_secret: process.env.CDEK_CLIENT_SECRET,
    }),
  });

  const { access_token } = await tokenResponse.json();

  // Запрос ПВЗ
  const pointsResponse = await fetch(
    `https://api.cdek.ru/v2/deliverypoints?city_code=${city}&type=${type || 'PVZ'}`,
    {
      headers: { Authorization: `Bearer ${access_token}` },
    }
  );

  const points = await pointsResponse.json();

  return res.json(points);
}
```

### Шаг 5: Интеграция в DeliverySelector

Замените `generateMockPickupPoints` на реальный запрос:

```javascript
// В DeliverySelector.jsx
const fetchCdekPoints = async (lat, lng) => {
  const response = await fetch(`/api/cdek-points?city=44&type=PVZ`);
  const data = await response.json();
  return data.map((point) => ({
    id: point.code,
    name: point.name,
    address: point.location.address_full,
    lat: point.location.latitude,
    lng: point.location.longitude,
    workHours: point.work_time,
    phone: point.phones?.[0]?.number,
    hasCard: point.have_cashless,
    hasFitting: point.is_dressing_room,
  }));
};
```

---

## 4. Boxberry API

### Шаг 1: Регистрация

1. Перейдите на [boxberry.ru/business](https://boxberry.ru/business)
2. Заключите договор на интеграцию
3. Получите API-токен в личном кабинете

### Шаг 2: Добавление переменных

```bash
BOXBERRY_TOKEN=ваш_токен
```

### Шаг 3: API-эндпоинт

```javascript
// api/boxberry-points.js
export default async function handler(req, res) {
  const { city } = req.query;
  const token = process.env.BOXBERRY_TOKEN;

  const response = await fetch(`https://api.boxberry.ru/v2/points?token=${token}&CityCode=${city}`);

  const data = await response.json();
  return res.json(data);
}
```

---

## 5. Telegram Bot

### Шаг 1: Создание бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Введите имя бота (например, `Arbarea Orders`)
4. Введите username (например, `arbarea_orders_bot`)
5. Скопируйте полученный токен

### Шаг 2: Получение Chat ID

1. Добавьте бота в группу администраторов
2. Отправьте сообщение в группу
3. Откройте URL:
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
   ```
4. Найдите `"chat":{"id":-1001234567890}` — это ваш Chat ID

### Шаг 3: Добавление переменных

```bash
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=-1001234567890
```

### Шаг 4: Тестирование

```bash
curl -X POST https://arbarea.ru/api/send-telegram \
  -H "Content-Type: application/json" \
  -d '{"message": "Тестовое сообщение из API"}'
```

---

## 6. Переменные окружения

### Полный список для Vercel

```bash
# === Firebase ===
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account",...}  # JSON в одну строку

# === Тинькофф ===
TINKOFF_TERMINAL_KEY=ваш_ключ_терминала
TINKOFF_PASSWORD=ваш_пароль

# === Email (Resend) ===
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxx

# === Telegram ===
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=-1001234567890

# === СДЭК (опционально) ===
CDEK_CLIENT_ID=ваш_client_id
CDEK_CLIENT_SECRET=ваш_client_secret
CDEK_TEST_MODE=false

# === Boxberry (опционально) ===
BOXBERRY_TOKEN=ваш_токен

# === AI Chat ===
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxx
```

### Как добавить в Vercel

1. Откройте [vercel.com](https://vercel.com) → Ваш проект
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте каждую переменную:
   - **Name**: имя переменной (например, `RESEND_API_KEY`)
   - **Value**: значение
   - **Environment**: Production, Preview, Development (выберите нужные)
4. Нажмите **Save**
5. Выполните **Redeploy** для применения изменений

### Локальная разработка

Создайте файл `.env.local` в корне проекта:

```bash
# .env.local
VITE_FIREBASE_API_KEY=...
VITE_FIREBASE_AUTH_DOMAIN=...
# ... другие VITE_ переменные для фронтенда

# Переменные для API (не VITE_)
FIREBASE_SERVICE_ACCOUNT_KEY=...
TINKOFF_TERMINAL_KEY=...
# ...
```

---

## 🔒 Важные замечания по безопасности

1. **Никогда** не коммитьте `.env.local` в git (он уже в `.gitignore`)
2. **Храните резервные копии** API-ключей в менеджере паролей
3. **Ротируйте ключи** регулярно (раз в 3-6 месяцев)
4. **Используйте разные ключи** для production и development
5. **Проверяйте подписи** всех входящих webhook-запросов

---

## ✅ Чеклист готовности

- [ ] Resend API настроен и работает
- [ ] Тинькофф webhook настроен и тестирован
- [ ] Telegram бот создан и отправляет уведомления
- [ ] СДЭК API подключен (опционально)
- [ ] Все переменные окружения добавлены в Vercel
- [ ] Redeploy выполнен после добавления переменных
- [ ] Тестовый заказ прошёл полный цикл

---

## 🆘 Устранение неполадок

### Email не отправляется

1. Проверьте `RESEND_API_KEY` в Vercel
2. Проверьте логи Vercel Functions
3. Убедитесь, что домен верифицирован в Resend

### Webhook не срабатывает

1. Проверьте URL в настройках Тинькофф
2. Проверьте, что URL доступен (не localhost)
3. Проверьте `TINKOFF_TERMINAL_KEY` и `TINKOFF_PASSWORD`

### Telegram не отправляет

1. Проверьте, что бот добавлен в группу
2. Проверьте Chat ID (должен быть с минусом для групп)
3. Проверьте токен бота

---

**Дата создания:** 2026-01-01
**Версия:** 1.0.0
