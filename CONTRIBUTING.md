# 🤝 Contributing to Arbarea

Спасибо за интерес к проекту! Этот документ описывает правила и процесс внесения изменений.

## 🔒 Система защиты от регресса

Проект оснащён автоматической системой контроля качества:

### Pre-commit Hooks (Husky)

```bash
# Автоматически при каждом коммите:
✓ Biome lint — проверка кода на ошибки
✓ Biome format — форматирование кода
✓ Prettier — форматирование CSS/JSON/MD
```

### Pre-push Hooks

```bash
# Автоматически перед push:
✓ Jest tests — запуск тестов
✓ Vite build — проверка сборки
```

### Commit Message Format

Используем [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<scope>): <description>

[optional body]
```

**Типы:**

- `feat` — новая функциональность
- `fix` — исправление бага
- `docs` — документация
- `style` — форматирование (без изменений логики)
- `refactor` — рефакторинг (без изменений поведения)
- `perf` — улучшение производительности
- `test` — добавление/изменение тестов
- `build` — сборка и зависимости
- `ci` — CI/CD конфигурация
- `chore` — прочие задачи

**Примеры:**

```bash
feat(auth): add phone verification
fix(cart): resolve quantity update bug
docs: update API integration guide
```

## 🚀 GitHub Actions CI/CD

### При каждом Push/PR:

1. **Quality** — Biome lint & format check
2. **Build** — Production build verification
3. **Test** — Unit tests with coverage
4. **Security** — npm audit & dependency review
5. **Lighthouse** — Performance metrics (на PR)

### При merge в main:

6. **Deploy** — Автоматический деплой на Vercel

## 📋 Checklist перед PR

- [ ] Код проходит `npm run check`
- [ ] Добавлены тесты для новой функциональности
- [ ] Обновлена документация (если нужно)
- [ ] Commit messages соответствуют формату
- [ ] Нет секретов в коде

## 🛠 Локальная разработка

```bash
# Установка
npm install

# Разработка
npm run dev

# Проверка качества
npm run check        # lint + format check
npm run check:all    # lint + format + build

# Тесты
npm test             # запуск тестов
npm run test:watch   # watch mode
npm run test:coverage # с покрытием

# Безопасность
npm run audit        # проверка зависимостей
npm run audit:fix    # автоисправление
```

## 🔧 VSCode Extensions

Рекомендуемые расширения для команды находятся в `.vscode/extensions.json`.
При открытии проекта VSCode предложит их установить.

**Ключевые:**

- Biome — линтинг и форматирование
- Tailwind CSS IntelliSense — автокомплит для Tailwind
- Error Lens — inline ошибки
- GitLens — расширенная работа с Git
- SonarLint — security и quality анализ

## 📊 Метрики качества

### Performance (Lighthouse CI)

- Performance: ≥70
- Accessibility: ≥90 (обязательно)
- Best Practices: ≥80
- SEO: ≥80

### Core Web Vitals

- FCP: < 2.5s
- LCP: < 4.0s
- CLS: < 0.1
- TBT: < 300ms

## 🔐 Безопасность

- Никогда не коммитьте секреты
- Используйте `.env.local` для локальных переменных
- Проверяйте `npm audit` перед PR
- CodeQL анализ запускается автоматически

---

При возникновении вопросов создавайте Issue в репозитории.
